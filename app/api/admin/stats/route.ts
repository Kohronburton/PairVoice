import {NextResponse} from 'next/server';
import {createClient} from '@supabase/supabase-js';
export async function GET(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return NextResponse.json({error:'Not configured'},{status:503});const db=createClient(url,key,{auth:{persistSession:false}});const [{count:users},{count:pairs},{count:completePairs},{count:referrals}]=await Promise.all([db.from('profiles').select('*',{count:'exact',head:true}),db.from('pairs').select('*',{count:'exact',head:true}),db.from('pairs').select('*',{count:'exact',head:true}).neq('status','invited'),db.from('profiles').select('*',{count:'exact',head:true}).not('referred_by','is',null)]);return NextResponse.json({users:users||0,pairs:pairs||0,completePairs:completePairs||0,referredUsers:referrals||0,target:20000,progress:Math.round(((users||0)/20000)*10000)/100});}
