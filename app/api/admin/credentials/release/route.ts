import{NextRequest,NextResponse}from'next/server';import{createClient}from'@supabase/supabase-js';
export async function POST(req:NextRequest){try{const{pairId}=await req.json();if(!pairId)return NextResponse.json({error:'pairId is required.'},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return NextResponse.json({error:'Not configured.'},{status:503});const db=createClient(url,key,{auth:{persistSession:false}});const{data,error}=await db.rpc('release_credential_bundle',{p_pair_id:pairId,p_actor_label:process.env.PAIRVOICE_ADMIN_USER||'ADMIN'});if(error){console.error(error);return NextResponse.json({error:'Credentials could not be released for this pair.'},{status:409})}return NextResponse.json({ok:true,assignmentId:data})}catch(e){console.error(e);return NextResponse.json({error:'Unable to release credentials.'},{status:500})}}
