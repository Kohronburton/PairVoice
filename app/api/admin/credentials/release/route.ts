import{NextRequest,NextResponse}from'next/server';import{requireAdmin}from'../../../../../lib/admin-server';
export async function POST(req:NextRequest){const admin=await requireAdmin(['SUPER_ADMIN','OPERATIONS']);if(!admin)return NextResponse.json({error:'Forbidden'},{status:403});try{const{pairId}=await req.json();if(!pairId)return NextResponse.json({error:'pairId is required.'},{status:400});const{data,error}=await admin.db.rpc('release_credential_bundle',{p_pair_id:pairId,p_actor_label:`ADMIN:${admin.role}`});if(error){console.error(error);return NextResponse.json({error:'Credentials could not be released for this pair.'},{status:409})}return NextResponse.json({ok:true,assignmentId:data})}catch(e){console.error(e);return NextResponse.json({error:'Unable to release credentials.'},{status:500})}}