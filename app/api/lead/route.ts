import{NextRequest,NextResponse}from'next/server';import{createClient}from'@supabase/supabase-js';import{clampText,isValidEmail,normalizeEmail,normalizeMarket}from'../../../lib/validation';
export async function POST(req:NextRequest){try{const b=await req.json(),email=normalizeEmail(b.email);if(!isValidEmail(email))return NextResponse.json({error:'Enter a valid email address.'},{status:400});if(b.consent!==true)return NextResponse.json({error:'Consent is required to receive PairVoice opportunities.'},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!url||!key)return NextResponse.json({error:'Lead signup is not configured.'},{status:503});const db=createClient(url,key,{auth:{persistSession:false}}),langs=Array.isArray(b.detected_languages)?b.detected_languages.map((x:unknown)=>String(x).slice(0,40)).slice(0,10):[];const{data,error}=await db.rpc('upsert_public_lead',{p_email:email,p_market_code:normalizeMarket(b.market_code),p_consent:true,p_detected_locale:clampText(b.detected_locale,40),p_detected_languages:langs,p_source:clampText(b.source,200),p_campaign_key:clampText(b.campaign_key||b.utm_campaign||'organic',120),p_landing_path:clampText(b.landing_path,500),p_referrer:clampText(b.referrer,1000),p_fbclid:clampText(b.fbclid,255),p_gclid:clampText(b.gclid,255),p_utm_source:clampText(b.utm_source,255),p_utm_medium:clampText(b.utm_medium,255),p_utm_campaign:clampText(b.utm_campaign,255),p_utm_content:clampText(b.utm_content,255),p_utm_term:clampText(b.utm_term,255)});if(error){console.error('lead rpc error',error);return NextResponse.json({error:'Unable to join the early-access list.'},{status:500})}return NextResponse.json(data||{ok:true})}catch(e){console.error(e);return NextResponse.json({error:'Unable to join the early-access list.'},{status:500})}}