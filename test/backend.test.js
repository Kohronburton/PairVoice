import test from'node:test';import assert from'node:assert/strict';import{campaigns,eligibility,createParticipant,createPair,joinPair,resetStore}from'../src/domain.js';test.beforeEach(resetStore);test('locked campaign economics are per pair',()=>{assert.equal(campaigns.us.publicPayoutAmount,60);assert.equal(campaigns.es.publicPayoutAmount,50);assert.equal(campaigns.us.payoutUnit,'PAIR');assert.equal(campaigns.es.payoutUnit,'PAIR')});test('US eligibility passes valid adult consent',()=>assert.equal(eligibility({market:'us',age:21,countryCode:'US',acceptsRecording:true}).eligible,true));test('eligibility rejects wrong country, minor, or no consent',()=>assert.deepEqual(eligibility({market:'es',age:17,countryCode:'US',acceptsRecording:false}).reasons,['AGE','COUNTRY','CONSENT']));test('participant starts partner pending and invite forms pair',()=>{const a=createParticipant({market:'us',name:'A',email:'a@example.com'});assert.equal(a.status,'PARTNER_PENDING');const pair=createPair({participantAId:a.id});const formed=joinPair(pair.inviteToken,{name:'B',email:'b@example.com'});assert.equal(formed.status,'PAIR_FORMED');assert.ok(formed.participantBId)});test('same participant cannot join own pair',()=>{const a=createParticipant({market:'us',name:'A',email:'a@example.com'}),pair=createPair({participantAId:a.id});assert.throws(()=>joinPair(pair.inviteToken,{name:'A',email:'a@example.com'}),/different participant/)})