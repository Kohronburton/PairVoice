import{describe,expect,it}from'vitest';import{canTransitionPair,stateProgress}from'./workflow';
describe('pair workflow',()=>{it('allows revenue path',()=>{const p=['PARTNER_PENDING','PAIRED','READINESS_PENDING','READY','RECORDING','SUBMITTED','INTERNAL_QA','CLIENT_QA','APPROVED','PAYABLE','PAID']as const;for(let i=0;i<p.length-1;i++)expect(canTransitionPair(p[i],p[i+1])).toBe(true)});it('blocks gate skipping',()=>{expect(canTransitionPair('SUBMITTED','PAYABLE')).toBe(false);expect(canTransitionPair('RECORDING','APPROVED')).toBe(false)});it('marks paid complete',()=>expect(stateProgress.PAID).toBe(100))});